source("renv/activate.R")
if (interactive()) {
  suppressMessages(require(devtools))
}
options(
  usethis.description = list(
    "Authors@R" = utils::person(
      "Umberto", "Minora",
      email = "umbertofilippo@tiscali.it",
      role = c("aut", "cre"),
      comment = c(ORCID = "0000-0003-2151-6500")
    ),
    License = "MIT + file LICENSE"
  )
)
