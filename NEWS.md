# RegEnRF 2.0.0

## Breaking changes

* `RegEnRF()` now has two new arguments to handle arguments passed along 
to `glmnet::glmnet()` and `randomForest::randomForest()` (respectively 
`glmnet.args` and `rf.args`).

# RegEnRF 1.0.0

* Initial CRAN submission.
